RewriteEngine On
RewriteBase /

# Exclude the homepage, style.css, and anything in /error/ from the redirection
RewriteCond %{REQUEST_URI} !^/$
RewriteCond %{REQUEST_URI} !^/style\.css$
RewriteCond %{REQUEST_URI} !^/error/
RewriteRule ^ / [R=302,L]
